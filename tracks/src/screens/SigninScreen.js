import { StyleSheet, Text } from 'react-native';

const styles = StyleSheet.create({
  title: {
    fontSize: 48,
  },
});

const SigninScreen = () => {
  return <Text style={styles.title}>SigninScreen</Text>;
};

export default SigninScreen;
