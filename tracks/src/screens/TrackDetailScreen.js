import { StyleSheet, Text } from 'react-native';

const styles = StyleSheet.create({
  title: {
    fontSize: 48,
  },
});

const TrackDetailScreen = () => {
  return <Text style={styles.title}>TrackDetailScreen</Text>;
};

export default TrackDetailScreen;
