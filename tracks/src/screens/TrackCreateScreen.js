import { StyleSheet, Text } from 'react-native';

const styles = StyleSheet.create({
  title: {
    fontSize: 48,
  },
});

const TrackCreateScreen = () => {
  return <Text style={styles.title}>TrackCreateScreen</Text>;
};

export default TrackCreateScreen;
